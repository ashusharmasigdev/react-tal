import React from 'react'
import ReactDOM from 'react-dom/client'
import {TALPage} from 'react-tal'

const root = ReactDOM.createRoot(document.getElementById('root') as HTMLElement)
root.render(
    <React.StrictMode>
    </React.StrictMode>,
)